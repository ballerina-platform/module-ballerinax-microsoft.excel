// AUTO-GENERATED FILE. DO NOT MODIFY.
// This file is auto-generated by the Ballerina OpenAPI tool.

// Copyright (c) 2026, WSO2 LLC. (http://www.wso2.com).
//
// WSO2 LLC. licenses this file to you under the Apache License,
// Version 2.0 (the "License"); you may not use this file except
// in compliance with the License.
// You may obtain a copy of the License at
//
// http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.

import ballerina/data.jsondata;
import ballerina/http;

# Microsoft Excel API. Subset of the Microsoft Graph v1.0 OpenAPI specification covering workbook, worksheet, range, table, chart and pivot-table operations on files stored in OneDrive and SharePoint.
public isolated client class Client {
    final http:Client clientEp;
    # Gets invoked to initialize the `connector`.
    #
    # + config - The configurations to be used when initializing the `connector` 
    # + serviceUrl - URL of the target service 
    # + return - An error if connector initialization failed 
    public isolated function init(ConnectionConfig config, string serviceUrl = "https://graph.microsoft.com/v1.0") returns error? {
        http:ClientConfiguration httpClientConfig = {auth: config.auth, httpVersion: config.httpVersion, http1Settings: config.http1Settings, http2Settings: config.http2Settings, timeout: config.timeout, forwarded: config.forwarded, followRedirects: config.followRedirects, poolConfig: config.poolConfig, cache: config.cache, compression: config.compression, circuitBreaker: config.circuitBreaker, retryConfig: config.retryConfig, cookieConfig: config.cookieConfig, responseLimits: config.responseLimits, secureSocket: config.secureSocket, proxy: config.proxy, socketConfig: config.socketConfig, validation: config.validation, laxDataBinding: config.laxDataBinding};
        self.clientEp = check new (serviceUrl, httpClientConfig);
    }

    # Get workbook from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved workbook 
    remote isolated function getWorkbook(string driveId, string driveItemId, GetWorkbookHeaders headers = {}, *GetWorkbookQueries queries) returns Workbook|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update workbook
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + payload - The workbook properties to update 
    # + return - The updated workbook 
    remote isolated function updateWorkbook(string driveId, string driveItemId, Workbook payload, UpdateWorkbookHeaders headers = {}) returns Workbook|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get application from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved Excel application 
    remote isolated function getApplication(string driveId, string driveItemId, GetApplicationHeaders headers = {}, *GetApplicationQueries queries) returns Application|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/application`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update Excel application
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + payload - The Excel application properties to update 
    # + return - The updated Excel application 
    remote isolated function updateApplication(string driveId, string driveItemId, Application payload, UpdateApplicationHeaders headers = {}) returns Application|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/application`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke action calculate
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function calculateApplication(string driveId, string driveItemId, CalculateApplicationRequest payload, CalculateApplicationHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/application/calculate`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get comments from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listComments(string driveId, string driveItemId, ListCommentsHeaders headers = {}, *ListCommentsQueries queries) returns CommentCollectionResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/comments`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create workbook comment
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + payload - The workbook comment to create 
    # + return - The created workbook comment 
    remote isolated function createComment(string driveId, string driveItemId, Comment payload, CreateCommentHeaders headers = {}) returns Comment|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/comments`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get comments from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookCommentId - The unique identifier of workbookComment
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved workbook comment 
    remote isolated function getComment(string driveId, string driveItemId, string workbookCommentId, GetCommentHeaders headers = {}, *GetCommentQueries queries) returns Comment|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/comments/${getEncodedUri(workbookCommentId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete workbook comment
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookCommentId - The unique identifier of workbookComment
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteComment(string driveId, string driveItemId, string workbookCommentId, DeleteCommentHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/comments/${getEncodedUri(workbookCommentId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update workbook comment
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookCommentId - The unique identifier of workbookComment
    # + headers - Headers to be sent with the request 
    # + payload - The workbook comment properties to update 
    # + return - The updated workbook comment 
    remote isolated function updateComment(string driveId, string driveItemId, string workbookCommentId, Comment payload, UpdateCommentHeaders headers = {}) returns Comment|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/comments/${getEncodedUri(workbookCommentId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get replies from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookCommentId - The unique identifier of workbookComment
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listReplies(string driveId, string driveItemId, string workbookCommentId, ListRepliesHeaders headers = {}, *ListRepliesQueries queries) returns CommentReplyCollectionResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/comments/${getEncodedUri(workbookCommentId)}/replies`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create comment reply
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookCommentId - The unique identifier of workbookComment
    # + headers - Headers to be sent with the request 
    # + payload - The comment reply to create 
    # + return - The created comment reply 
    remote isolated function createReply(string driveId, string driveItemId, string workbookCommentId, CommentReply payload, CreateReplyHeaders headers = {}) returns CommentReply|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/comments/${getEncodedUri(workbookCommentId)}/replies`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get replies from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookCommentId - The unique identifier of workbookComment
    # + workbookCommentReplyId - The unique identifier of workbookCommentReply
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved comment reply 
    remote isolated function getReply(string driveId, string driveItemId, string workbookCommentId, string workbookCommentReplyId, GetReplyHeaders headers = {}, *GetReplyQueries queries) returns CommentReply|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/comments/${getEncodedUri(workbookCommentId)}/replies/${getEncodedUri(workbookCommentReplyId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete comment reply
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookCommentId - The unique identifier of workbookComment
    # + workbookCommentReplyId - The unique identifier of workbookCommentReply
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteReply(string driveId, string driveItemId, string workbookCommentId, string workbookCommentReplyId, DeleteReplyHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/comments/${getEncodedUri(workbookCommentId)}/replies/${getEncodedUri(workbookCommentReplyId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update comment reply
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookCommentId - The unique identifier of workbookComment
    # + workbookCommentReplyId - The unique identifier of workbookCommentReply
    # + headers - Headers to be sent with the request 
    # + payload - The comment reply properties to update 
    # + return - The updated comment reply 
    remote isolated function updateReply(string driveId, string driveItemId, string workbookCommentId, string workbookCommentReplyId, CommentReply payload, UpdateReplyHeaders headers = {}) returns CommentReply|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/comments/${getEncodedUri(workbookCommentId)}/replies/${getEncodedUri(workbookCommentReplyId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get the number of the resource
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookCommentId - The unique identifier of workbookComment
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function countReplies(string driveId, string driveItemId, string workbookCommentId, CountRepliesHeaders headers = {}, *CountRepliesQueries queries) returns string|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/comments/${getEncodedUri(workbookCommentId)}/replies/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get the number of the resource
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function countComments(string driveId, string driveItemId, CountCommentsHeaders headers = {}, *CountCommentsQueries queries) returns string|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/comments/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke action closeSession
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function closeSession(string driveId, string driveItemId, CloseSessionHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/closeSession`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action createSession
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - The created workbook session 
    remote isolated function createSession(string driveId, string driveItemId, CreateSessionRequest payload, map<string|string[]> headers = {}) returns SessionInfoResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/createSession`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Invoke action refreshSession
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function refreshSession(string driveId, string driveItemId, RefreshSessionHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/refreshSession`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke function sessionInfoResource
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + 'key - Usage: key='{key}'
    # + headers - Headers to be sent with the request 
    # + return - The workbook session information 
    remote isolated function getSessionInfo(string driveId, string driveItemId, string 'key, GetSessionInfoHeaders headers = {}) returns SessionInfoResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/sessionInfoResource(key='${getEncodedUri('key)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke function tableRowOperationResult
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + 'key - Usage: key='{key}'
    # + headers - Headers to be sent with the request 
    # + return - The result of the table row operation 
    remote isolated function getTableRowOperationResult(string driveId, string driveItemId, string 'key, GetTableRowOperationResultHeaders headers = {}) returns TableRowOperationResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/tableRowOperationResult(key='${getEncodedUri('key)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get names from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listNamedItems(string driveId, string driveItemId, ListNamedItemsHeaders headers = {}, *ListNamedItemsQueries queries) returns NamedItemCollectionResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/names`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create named item
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + payload - The named item to create 
    # + return - The created named item 
    remote isolated function createNamedItem(string driveId, string driveItemId, NamedItem payload, CreateNamedItemHeaders headers = {}) returns NamedItem|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/names`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get names from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookNamedItemId - The unique identifier of workbookNamedItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved named item 
    remote isolated function getNamedItem(string driveId, string driveItemId, string workbookNamedItemId, GetNamedItemHeaders headers = {}, *GetNamedItemQueries queries) returns NamedItem|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/names/${getEncodedUri(workbookNamedItemId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete named item
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookNamedItemId - The unique identifier of workbookNamedItem
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteNamedItem(string driveId, string driveItemId, string workbookNamedItemId, DeleteNamedItemHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/names/${getEncodedUri(workbookNamedItemId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update named item
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookNamedItemId - The unique identifier of workbookNamedItem
    # + headers - Headers to be sent with the request 
    # + payload - The named item properties to update 
    # + return - The updated named item 
    remote isolated function updateNamedItem(string driveId, string driveItemId, string workbookNamedItemId, NamedItem payload, UpdateNamedItemHeaders headers = {}) returns NamedItem|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/names/${getEncodedUri(workbookNamedItemId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke function range
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookNamedItemId - The unique identifier of workbookNamedItem
    # + headers - Headers to be sent with the request 
    # + return - The retrieved named item range 
    remote isolated function getNamedItemRange(string driveId, string driveItemId, string workbookNamedItemId, GetNamedItemRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/names/${getEncodedUri(workbookNamedItemId)}/range()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get worksheet from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookNamedItemId - The unique identifier of workbookNamedItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved worksheet 
    remote isolated function getNamedItemWorksheet(string driveId, string driveItemId, string workbookNamedItemId, GetNamedItemWorksheetHeaders headers = {}, *GetNamedItemWorksheetQueries queries) returns Worksheet|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/names/${getEncodedUri(workbookNamedItemId)}/worksheet`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get the number of the resource
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function countNamedItems(string driveId, string driveItemId, CountNamedItemsHeaders headers = {}, *CountNamedItemsQueries queries) returns string|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/names/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke action add
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - The created named item 
    remote isolated function addNamedItem(string driveId, string driveItemId, AddNamedItemRequest payload, AddNamedItemHeaders headers = {}) returns NamedItemResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/names/add`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action addFormulaLocal
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - The created named item 
    remote isolated function addNamedItemFormulaLocal(string driveId, string driveItemId, AddNamedItemFormulaLocalRequest payload, AddNamedItemFormulaLocalHeaders headers = {}) returns NamedItemResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/names/addFormulaLocal`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get operations from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listOperations(string driveId, string driveItemId, ListOperationsHeaders headers = {}, *ListOperationsQueries queries) returns OperationCollectionResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/operations`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create workbook operation
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + payload - The workbook operation to create 
    # + return - The created workbook operation 
    remote isolated function createOperation(string driveId, string driveItemId, Operation payload, CreateOperationHeaders headers = {}) returns Operation|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/operations`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get operations from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookOperationId - The unique identifier of workbookOperation
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved workbook operation 
    remote isolated function getOperation(string driveId, string driveItemId, string workbookOperationId, GetOperationHeaders headers = {}, *GetOperationQueries queries) returns Operation|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/operations/${getEncodedUri(workbookOperationId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete workbook operation
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookOperationId - The unique identifier of workbookOperation
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteOperation(string driveId, string driveItemId, string workbookOperationId, DeleteOperationHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/operations/${getEncodedUri(workbookOperationId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update workbook operation
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookOperationId - The unique identifier of workbookOperation
    # + headers - Headers to be sent with the request 
    # + payload - The workbook operation properties to update 
    # + return - The updated workbook operation 
    remote isolated function updateOperation(string driveId, string driveItemId, string workbookOperationId, Operation payload, UpdateOperationHeaders headers = {}) returns Operation|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/operations/${getEncodedUri(workbookOperationId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get the number of the resource
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function countOperations(string driveId, string driveItemId, CountOperationsHeaders headers = {}, *CountOperationsQueries queries) returns string|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/operations/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get tables from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listTables(string driveId, string driveItemId, ListTablesHeaders headers = {}, *ListTablesQueries queries) returns TableCollectionResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/tables`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create table
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + payload - The table to create 
    # + return - The created table 
    remote isolated function createTable(string driveId, string driveItemId, Table payload, CreateTableHeaders headers = {}) returns Table|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/tables`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get worksheets from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listWorksheets(string driveId, string driveItemId, ListWorksheetsHeaders headers = {}, *ListWorksheetsQueries queries) returns WorksheetCollectionResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create worksheet
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + payload - The worksheet to create 
    # + return - The created worksheet 
    remote isolated function createWorksheet(string driveId, string driveItemId, Worksheet payload, CreateWorksheetHeaders headers = {}) returns Worksheet|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get worksheets from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved worksheet 
    remote isolated function getWorksheet(string driveId, string driveItemId, string workbookWorksheetId, GetWorksheetHeaders headers = {}, *GetWorksheetQueries queries) returns Worksheet|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete worksheet
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteWorksheet(string driveId, string driveItemId, string workbookWorksheetId, DeleteWorksheetHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update worksheet
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - The worksheet properties to update 
    # + return - The updated worksheet 
    remote isolated function updateWorksheet(string driveId, string driveItemId, string workbookWorksheetId, Worksheet payload, UpdateWorksheetHeaders headers = {}) returns Worksheet|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get charts from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listCharts(string driveId, string driveItemId, string workbookWorksheetId, ListChartsHeaders headers = {}, *ListChartsQueries queries) returns ChartCollectionResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create chart
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - The chart to create 
    # + return - The created chart 
    remote isolated function createChart(string driveId, string driveItemId, string workbookWorksheetId, Chart payload, CreateChartHeaders headers = {}) returns Chart|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get charts from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved chart 
    remote isolated function getChart(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, GetChartHeaders headers = {}, *GetChartQueries queries) returns Chart|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete chart
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteChart(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, DeleteChartHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update chart
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + payload - The chart properties to update 
    # + return - The updated chart 
    remote isolated function updateChart(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, Chart payload, UpdateChartHeaders headers = {}) returns Chart|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get axes from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved chart axes 
    remote isolated function getChartAxes(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, GetChartAxesHeaders headers = {}, *GetChartAxesQueries queries) returns ChartAxes|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/axes`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update chart axes
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + payload - The chart axes properties to update 
    # + return - The updated chart axes 
    remote isolated function updateChartAxes(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, ChartAxes payload, UpdateChartAxesHeaders headers = {}) returns ChartAxes|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/axes`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get categoryAxis from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved category axis 
    remote isolated function getCategoryAxis(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, GetCategoryAxisHeaders headers = {}, *GetCategoryAxisQueries queries) returns ChartAxis|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/axes/categoryAxis`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update category axis
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + payload - The category axis properties to update 
    # + return - The updated category axis 
    remote isolated function updateCategoryAxis(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, ChartAxis payload, UpdateCategoryAxisHeaders headers = {}) returns ChartAxis|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/axes/categoryAxis`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get seriesAxis from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved series axis 
    remote isolated function getSeriesAxis(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, GetSeriesAxisHeaders headers = {}, *GetSeriesAxisQueries queries) returns ChartAxis|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/axes/seriesAxis`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update series axis
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + payload - The series axis properties to update 
    # + return - The updated series axis 
    remote isolated function updateSeriesAxis(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, ChartAxis payload, UpdateSeriesAxisHeaders headers = {}) returns ChartAxis|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/axes/seriesAxis`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get valueAxis from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved value axis 
    remote isolated function getValueAxis(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, GetValueAxisHeaders headers = {}, *GetValueAxisQueries queries) returns ChartAxis|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/axes/valueAxis`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update value axis
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + payload - The value axis properties to update 
    # + return - The updated value axis 
    remote isolated function updateValueAxis(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, ChartAxis payload, UpdateValueAxisHeaders headers = {}) returns ChartAxis|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/axes/valueAxis`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get dataLabels from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved chart data labels 
    remote isolated function getDataLabels(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, GetDataLabelsHeaders headers = {}, *GetDataLabelsQueries queries) returns ChartDataLabels|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/dataLabels`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update chart data labels
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + payload - The chart data labels properties to update 
    # + return - The updated chart data labels 
    remote isolated function updateDataLabels(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, ChartDataLabels payload, UpdateDataLabelsHeaders headers = {}) returns ChartDataLabels|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/dataLabels`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get format from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved chart format 
    remote isolated function getChartFormat(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, GetChartFormatHeaders headers = {}, *GetChartFormatQueries queries) returns ChartAreaFormat|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/format`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update chart format
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + payload - The chart format properties to update 
    # + return - The updated chart format 
    remote isolated function updateChartFormat(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, ChartAreaFormat payload, UpdateChartFormatHeaders headers = {}) returns ChartAreaFormat|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/format`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get legend from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved chart legend 
    remote isolated function getLegend(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, GetLegendHeaders headers = {}, *GetLegendQueries queries) returns ChartLegend|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/legend`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update chart legend
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + payload - The chart legend properties to update 
    # + return - The updated chart legend 
    remote isolated function updateLegend(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, ChartLegend payload, UpdateLegendHeaders headers = {}) returns ChartLegend|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/legend`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke function image
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + return - The chart image as a base64-encoded string 
    remote isolated function getChartImage(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, GetChartImageHeaders headers = {}) returns ChartImageResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/image()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke function image
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + width - Usage: width={width}
    # + height - Usage: height={height}
    # + fittingMode - Usage: fittingMode='{fittingMode}'
    # + headers - Headers to be sent with the request 
    # + return - The chart image as a base64-encoded string, scaled to the given width and height using the specified fitting mode 
    remote isolated function getChartImageFitted(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, decimal width, decimal height, string fittingMode, GetChartImageFittedHeaders headers = {}) returns ChartImageResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/image(width=${getEncodedUri(width)},height=${getEncodedUri(height)},fittingMode='${getEncodedUri(fittingMode)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke function image
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + width - Usage: width={width}
    # + height - Usage: height={height}
    # + headers - Headers to be sent with the request 
    # + return - The chart image as a base64-encoded string, scaled to the given width and height 
    remote isolated function getChartImageBySize(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, decimal width, decimal height, GetChartImageBySizeHeaders headers = {}) returns ChartImageResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/image(width=${getEncodedUri(width)},height=${getEncodedUri(height)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke function image
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + width - Usage: width={width}
    # + headers - Headers to be sent with the request 
    # + return - The chart image as a base64-encoded string, scaled to the given width 
    remote isolated function getChartImageByWidth(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, decimal width, GetChartImageByWidthHeaders headers = {}) returns ChartImageResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/image(width=${getEncodedUri(width)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke action setData
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function setChartData(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, SetChartDataRequest payload, SetChartDataHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/setData`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action setPosition
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function setChartPosition(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, SetChartPositionRequest payload, SetChartPositionHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/setPosition`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get series from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listChartSeries(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, ListChartSeriesHeaders headers = {}, *ListChartSeriesQueries queries) returns ChartSeriesCollectionResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/series`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create chart series
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + payload - The chart series to create 
    # + return - The created chart series 
    remote isolated function createChartSeries(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, ChartSeries payload, CreateChartSeriesHeaders headers = {}) returns ChartSeries|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/series`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get series from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + workbookChartSeriesId - The unique identifier of workbookChartSeries
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved chart series 
    remote isolated function getChartSeries(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, string workbookChartSeriesId, GetChartSeriesHeaders headers = {}, *GetChartSeriesQueries queries) returns ChartSeries|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/series/${getEncodedUri(workbookChartSeriesId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete chart series
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + workbookChartSeriesId - The unique identifier of workbookChartSeries
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteChartSeries(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, string workbookChartSeriesId, DeleteChartSeriesHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/series/${getEncodedUri(workbookChartSeriesId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update chart series
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + workbookChartSeriesId - The unique identifier of workbookChartSeries
    # + headers - Headers to be sent with the request 
    # + payload - The chart series properties to update 
    # + return - The updated chart series 
    remote isolated function updateChartSeries(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, string workbookChartSeriesId, ChartSeries payload, UpdateChartSeriesHeaders headers = {}) returns ChartSeries|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/series/${getEncodedUri(workbookChartSeriesId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke function count
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + return - The number of chart series 
    remote isolated function countChartSeries(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, CountChartSeriesHeaders headers = {}) returns CountResult|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/series/count()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get title from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved chart title 
    remote isolated function getChartTitle(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, GetChartTitleHeaders headers = {}, *GetChartTitleQueries queries) returns ChartTitle|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/title`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update chart title
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + payload - The chart title properties to update 
    # + return - The updated chart title 
    remote isolated function updateChartTitle(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, ChartTitle payload, UpdateChartTitleHeaders headers = {}) returns ChartTitle|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/title`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get worksheet from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookChartId - The unique identifier of workbookChart
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved worksheet 
    remote isolated function getChartWorksheet(string driveId, string driveItemId, string workbookWorksheetId, string workbookChartId, GetChartWorksheetHeaders headers = {}, *GetChartWorksheetQueries queries) returns Worksheet|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/${getEncodedUri(workbookChartId)}/worksheet`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke action add
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - The created chart 
    remote isolated function addChart(string driveId, string driveItemId, string workbookWorksheetId, AddChartRequest payload, AddChartHeaders headers = {}) returns AddChartResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/add`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke function count
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + return - The number of charts 
    remote isolated function countCharts(string driveId, string driveItemId, string workbookWorksheetId, CountChartsHeaders headers = {}) returns CountResult|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/charts/count()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke function cell
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + row - Usage: row={row}
    # + column - Usage: column={column}
    # + headers - Headers to be sent with the request 
    # + return - The retrieved cell 
    remote isolated function getCell(string driveId, string driveItemId, string workbookWorksheetId, decimal row, decimal column, GetCellHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/cell(row=${getEncodedUri(row)},column=${getEncodedUri(column)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke function range
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + return - The retrieved range 
    remote isolated function getRange(string driveId, string driveItemId, string workbookWorksheetId, GetRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get format from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved range format 
    remote isolated function getRangeFormat(string driveId, string driveItemId, string workbookWorksheetId, GetRangeFormatHeaders headers = {}, *GetRangeFormatQueries queries) returns RangeFormat|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range()/format`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update range format
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - The range format properties to update 
    # + return - The updated range format 
    remote isolated function updateRangeFormat(string driveId, string driveItemId, string workbookWorksheetId, RangeFormat payload, UpdateRangeFormatHeaders headers = {}) returns RangeFormat|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range()/format`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke action clear
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function clearRange(string driveId, string driveItemId, string workbookWorksheetId, ClearRangeRequest payload, ClearRangeHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range()/clear`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action delete
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function deleteRange(string driveId, string driveItemId, string workbookWorksheetId, DeleteRangeRequest payload, DeleteRangeHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range()/delete`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action insert
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - The inserted range 
    remote isolated function insertRange(string driveId, string driveItemId, string workbookWorksheetId, DeleteRangeRequest payload, InsertRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range()/insert`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action merge
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function mergeRange(string driveId, string driveItemId, string workbookWorksheetId, MergeRangeRequest payload, MergeRangeHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range()/merge`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action unmerge
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function unmergeRange(string driveId, string driveItemId, string workbookWorksheetId, UnmergeRangeHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range()/unmerge`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get sort from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved range sort 
    remote isolated function getRangeSort(string driveId, string driveItemId, string workbookWorksheetId, GetRangeSortHeaders headers = {}, *GetRangeSortQueries queries) returns RangeSort|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range()/sort`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update range sort
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - The range sort properties to update 
    # + return - The updated range sort 
    remote isolated function updateRangeSort(string driveId, string driveItemId, string workbookWorksheetId, RangeSort payload, UpdateRangeSortHeaders headers = {}) returns RangeSort|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range()/sort`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke function range
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + address - Usage: address='{address}'
    # + headers - Headers to be sent with the request 
    # + return - The retrieved range at the given address 
    remote isolated function getRangeByAddress(string driveId, string driveItemId, string workbookWorksheetId, string? address, GetRangeByAddressHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range(address='${getEncodedUri(address)}')`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get format from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + address - Usage: address='{address}'
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved addressed range format 
    remote isolated function getRangeByAddressFormat(string driveId, string driveItemId, string workbookWorksheetId, string? address, GetRangeByAddressFormatHeaders headers = {}, *GetRangeByAddressFormatQueries queries) returns RangeFormat|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range(address='${getEncodedUri(address)}')/format`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update addressed range format
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + address - Usage: address='{address}'
    # + headers - Headers to be sent with the request 
    # + payload - The addressed range format properties to update 
    # + return - The updated format of the range at the given address 
    remote isolated function updateRangeByAddressFormat(string driveId, string driveItemId, string workbookWorksheetId, string? address, RangeFormat payload, UpdateRangeByAddressFormatHeaders headers = {}) returns RangeFormat|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range(address='${getEncodedUri(address)}')/format`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke action clear
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + address - Usage: address='{address}'
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function clearRangeByAddress(string driveId, string driveItemId, string workbookWorksheetId, string? address, ClearRangeRequest payload, ClearRangeByAddressHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range(address='${getEncodedUri(address)}')/clear`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action delete
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + address - Usage: address='{address}'
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function deleteRangeByAddress(string driveId, string driveItemId, string workbookWorksheetId, string? address, DeleteRangeRequest payload, DeleteRangeByAddressHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range(address='${getEncodedUri(address)}')/delete`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action insert
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + address - Usage: address='{address}'
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - The inserted range at the given address 
    remote isolated function insertRangeByAddress(string driveId, string driveItemId, string workbookWorksheetId, string? address, DeleteRangeRequest payload, InsertRangeByAddressHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range(address='${getEncodedUri(address)}')/insert`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action merge
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + address - Usage: address='{address}'
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function mergeRangeByAddress(string driveId, string driveItemId, string workbookWorksheetId, string? address, MergeRangeRequest payload, MergeRangeByAddressHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range(address='${getEncodedUri(address)}')/merge`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action unmerge
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + address - Usage: address='{address}'
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function unmergeRangeByAddress(string driveId, string driveItemId, string workbookWorksheetId, string? address, UnmergeRangeByAddressHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range(address='${getEncodedUri(address)}')/unmerge`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get sort from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + address - Usage: address='{address}'
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved addressed range sort 
    remote isolated function getRangeByAddressSort(string driveId, string driveItemId, string workbookWorksheetId, string? address, GetRangeByAddressSortHeaders headers = {}, *GetRangeByAddressSortQueries queries) returns RangeSort|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range(address='${getEncodedUri(address)}')/sort`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update addressed range sort
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + address - Usage: address='{address}'
    # + headers - Headers to be sent with the request 
    # + payload - The addressed range sort properties to update 
    # + return - The updated sort of the range at the given address 
    remote isolated function updateRangeByAddressSort(string driveId, string driveItemId, string workbookWorksheetId, string? address, RangeSort payload, UpdateRangeByAddressSortHeaders headers = {}) returns RangeSort|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/range(address='${getEncodedUri(address)}')/sort`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke function usedRange
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + return - The retrieved used range 
    remote isolated function getUsedRange(string driveId, string driveItemId, string workbookWorksheetId, GetUsedRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/usedRange()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get format from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved used range format 
    remote isolated function getUsedRangeFormat(string driveId, string driveItemId, string workbookWorksheetId, GetUsedRangeFormatHeaders headers = {}, *GetUsedRangeFormatQueries queries) returns RangeFormat|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/usedRange()/format`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update used range format
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - The used range format properties to update 
    # + return - The updated used range format 
    remote isolated function updateUsedRangeFormat(string driveId, string driveItemId, string workbookWorksheetId, RangeFormat payload, UpdateUsedRangeFormatHeaders headers = {}) returns RangeFormat|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/usedRange()/format`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke action clear
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function clearUsedRange(string driveId, string driveItemId, string workbookWorksheetId, ClearRangeRequest payload, ClearUsedRangeHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/usedRange()/clear`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action delete
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function deleteUsedRange(string driveId, string driveItemId, string workbookWorksheetId, DeleteRangeRequest payload, DeleteUsedRangeHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/usedRange()/delete`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action insert
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - The inserted range 
    remote isolated function insertUsedRange(string driveId, string driveItemId, string workbookWorksheetId, DeleteRangeRequest payload, InsertUsedRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/usedRange()/insert`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action merge
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function mergeUsedRange(string driveId, string driveItemId, string workbookWorksheetId, MergeRangeRequest payload, MergeUsedRangeHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/usedRange()/merge`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action unmerge
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function unmergeUsedRange(string driveId, string driveItemId, string workbookWorksheetId, UnmergeUsedRangeHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/usedRange()/unmerge`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get sort from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved used range sort 
    remote isolated function getUsedRangeSort(string driveId, string driveItemId, string workbookWorksheetId, GetUsedRangeSortHeaders headers = {}, *GetUsedRangeSortQueries queries) returns RangeSort|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/usedRange()/sort`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update used range sort
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - The used range sort properties to update 
    # + return - The updated used range sort 
    remote isolated function updateUsedRangeSort(string driveId, string driveItemId, string workbookWorksheetId, RangeSort payload, UpdateUsedRangeSortHeaders headers = {}) returns RangeSort|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/usedRange()/sort`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke function usedRange
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + valuesOnly - Usage: valuesOnly={valuesOnly}
    # + headers - Headers to be sent with the request 
    # + return - The retrieved used range, restricted to cells that contain values 
    remote isolated function getUsedRangeValuesOnly(string driveId, string driveItemId, string workbookWorksheetId, boolean valuesOnly, GetUsedRangeValuesOnlyHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/usedRange(valuesOnly=${getEncodedUri(valuesOnly)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get names from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listWorksheetNamedItems(string driveId, string driveItemId, string workbookWorksheetId, ListWorksheetNamedItemsHeaders headers = {}, *ListWorksheetNamedItemsQueries queries) returns NamedItemCollectionResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/names`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create worksheet named item
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - The worksheet named item to create 
    # + return - The created worksheet named item 
    remote isolated function createWorksheetNamedItem(string driveId, string driveItemId, string workbookWorksheetId, NamedItem payload, CreateWorksheetNamedItemHeaders headers = {}) returns NamedItem|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/names`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get names from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookNamedItemId - The unique identifier of workbookNamedItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved worksheet named item 
    remote isolated function getWorksheetNamedItem(string driveId, string driveItemId, string workbookWorksheetId, string workbookNamedItemId, GetWorksheetNamedItemHeaders headers = {}, *GetWorksheetNamedItemQueries queries) returns NamedItem|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/names/${getEncodedUri(workbookNamedItemId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete worksheet named item
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookNamedItemId - The unique identifier of workbookNamedItem
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteWorksheetNamedItem(string driveId, string driveItemId, string workbookWorksheetId, string workbookNamedItemId, DeleteWorksheetNamedItemHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/names/${getEncodedUri(workbookNamedItemId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update worksheet named item
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookNamedItemId - The unique identifier of workbookNamedItem
    # + headers - Headers to be sent with the request 
    # + payload - The worksheet named item properties to update 
    # + return - The updated worksheet named item 
    remote isolated function updateWorksheetNamedItem(string driveId, string driveItemId, string workbookWorksheetId, string workbookNamedItemId, NamedItem payload, UpdateWorksheetNamedItemHeaders headers = {}) returns NamedItem|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/names/${getEncodedUri(workbookNamedItemId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke function range
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookNamedItemId - The unique identifier of workbookNamedItem
    # + headers - Headers to be sent with the request 
    # + return - The retrieved worksheet named item range 
    remote isolated function getWorksheetNamedItemRange(string driveId, string driveItemId, string workbookWorksheetId, string workbookNamedItemId, GetWorksheetNamedItemRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/names/${getEncodedUri(workbookNamedItemId)}/range()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get worksheet from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookNamedItemId - The unique identifier of workbookNamedItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved worksheet 
    remote isolated function getWorksheetNamedItemWorksheet(string driveId, string driveItemId, string workbookWorksheetId, string workbookNamedItemId, GetWorksheetNamedItemWorksheetHeaders headers = {}, *GetWorksheetNamedItemWorksheetQueries queries) returns Worksheet|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/names/${getEncodedUri(workbookNamedItemId)}/worksheet`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get the number of the resource
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function countWorksheetNamedItems(string driveId, string driveItemId, string workbookWorksheetId, CountWorksheetNamedItemsHeaders headers = {}, *CountWorksheetNamedItemsQueries queries) returns string|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/names/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke action add
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - The created worksheet named item 
    remote isolated function addWorksheetNamedItem(string driveId, string driveItemId, string workbookWorksheetId, AddNamedItemRequest payload, AddWorksheetNamedItemHeaders headers = {}) returns NamedItemResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/names/add`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action addFormulaLocal
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - The created worksheet named item 
    remote isolated function addWorksheetNamedItemFormulaLocal(string driveId, string driveItemId, string workbookWorksheetId, AddNamedItemFormulaLocalRequest payload, AddWorksheetNamedItemFormulaLocalHeaders headers = {}) returns NamedItemResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/names/addFormulaLocal`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get pivotTables from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listPivotTables(string driveId, string driveItemId, string workbookWorksheetId, ListPivotTablesHeaders headers = {}, *ListPivotTablesQueries queries) returns PivotTableCollectionResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/pivotTables`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create PivotTable
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - The PivotTable to create 
    # + return - The created PivotTable 
    remote isolated function createPivotTable(string driveId, string driveItemId, string workbookWorksheetId, PivotTable payload, CreatePivotTableHeaders headers = {}) returns PivotTable|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/pivotTables`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get pivotTables from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookPivotTableId - The unique identifier of workbookPivotTable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved PivotTable 
    remote isolated function getPivotTable(string driveId, string driveItemId, string workbookWorksheetId, string workbookPivotTableId, GetPivotTableHeaders headers = {}, *GetPivotTableQueries queries) returns PivotTable|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/pivotTables/${getEncodedUri(workbookPivotTableId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete PivotTable
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookPivotTableId - The unique identifier of workbookPivotTable
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deletePivotTable(string driveId, string driveItemId, string workbookWorksheetId, string workbookPivotTableId, DeletePivotTableHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/pivotTables/${getEncodedUri(workbookPivotTableId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update PivotTable
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookPivotTableId - The unique identifier of workbookPivotTable
    # + headers - Headers to be sent with the request 
    # + payload - The PivotTable properties to update 
    # + return - The updated PivotTable 
    remote isolated function updatePivotTable(string driveId, string driveItemId, string workbookWorksheetId, string workbookPivotTableId, PivotTable payload, UpdatePivotTableHeaders headers = {}) returns PivotTable|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/pivotTables/${getEncodedUri(workbookPivotTableId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke action refresh
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookPivotTableId - The unique identifier of workbookPivotTable
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function refreshPivotTable(string driveId, string driveItemId, string workbookWorksheetId, string workbookPivotTableId, RefreshPivotTableHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/pivotTables/${getEncodedUri(workbookPivotTableId)}/refresh`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get worksheet from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookPivotTableId - The unique identifier of workbookPivotTable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved worksheet 
    remote isolated function getPivotTableWorksheet(string driveId, string driveItemId, string workbookWorksheetId, string workbookPivotTableId, GetPivotTableWorksheetHeaders headers = {}, *GetPivotTableWorksheetQueries queries) returns Worksheet|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/pivotTables/${getEncodedUri(workbookPivotTableId)}/worksheet`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get the number of the resource
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function countPivotTables(string driveId, string driveItemId, string workbookWorksheetId, CountPivotTablesHeaders headers = {}, *CountPivotTablesQueries queries) returns string|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/pivotTables/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke action refreshAll
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function refreshAllPivotTables(string driveId, string driveItemId, string workbookWorksheetId, RefreshAllPivotTablesHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/pivotTables/refreshAll`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get protection from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved worksheet protection 
    remote isolated function getProtection(string driveId, string driveItemId, string workbookWorksheetId, GetProtectionHeaders headers = {}, *GetProtectionQueries queries) returns WorksheetProtection|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/protection`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update worksheet protection
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - The worksheet protection properties to update 
    # + return - The updated worksheet protection 
    remote isolated function updateProtection(string driveId, string driveItemId, string workbookWorksheetId, WorksheetProtection payload, UpdateProtectionHeaders headers = {}) returns WorksheetProtection|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/protection`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke action protect
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function protectWorksheet(string driveId, string driveItemId, string workbookWorksheetId, ProtectWorksheetRequest payload, ProtectWorksheetHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/protection/protect`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action unprotect
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function unprotectWorksheet(string driveId, string driveItemId, string workbookWorksheetId, UnprotectWorksheetHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/protection/unprotect`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get tables from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listWorksheetTables(string driveId, string driveItemId, string workbookWorksheetId, ListWorksheetTablesHeaders headers = {}, *ListWorksheetTablesQueries queries) returns TableCollectionResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create worksheet table
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - The worksheet table to create 
    # + return - The created worksheet table 
    remote isolated function createWorksheetTable(string driveId, string driveItemId, string workbookWorksheetId, Table payload, CreateWorksheetTableHeaders headers = {}) returns Table|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get tables from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved worksheet table 
    remote isolated function getWorksheetTable(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, GetWorksheetTableHeaders headers = {}, *GetWorksheetTableQueries queries) returns Table|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete worksheet table
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteWorksheetTable(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, DeleteWorksheetTableHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update worksheet table
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + payload - The worksheet table properties to update 
    # + return - The updated worksheet table 
    remote isolated function updateWorksheetTable(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, Table payload, UpdateWorksheetTableHeaders headers = {}) returns Table|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get columns from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listColumns(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, ListColumnsHeaders headers = {}, *ListColumnsQueries queries) returns TableColumnCollectionResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create table column
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + payload - The table column to create 
    # + return - The created table column 
    remote isolated function createColumn(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, TableColumn payload, CreateColumnHeaders headers = {}) returns TableColumn|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get columns from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved table column 
    remote isolated function getColumn(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, GetColumnHeaders headers = {}, *GetColumnQueries queries) returns TableColumn|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete table column
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteColumn(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, DeleteColumnHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update table column
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + payload - The table column properties to update 
    # + return - The updated table column 
    remote isolated function updateColumn(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, TableColumn payload, UpdateColumnHeaders headers = {}) returns TableColumn|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Get filter from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved table column filter 
    remote isolated function getColumnFilter(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, GetColumnFilterHeaders headers = {}, *GetColumnFilterQueries queries) returns Filter|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/filter`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update table column filter
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + payload - The table column filter properties to update 
    # + return - The updated table column filter 
    remote isolated function updateColumnFilter(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, Filter payload, UpdateColumnFilterHeaders headers = {}) returns Filter|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/filter`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke action apply
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function applyColumnFilter(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, ApplyFilterRequest payload, ApplyColumnFilterHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/filter/apply`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action applyBottomItemsFilter
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function applyBottomItemsFilter(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, ApplyBottomItemsFilterRequest payload, ApplyBottomItemsFilterHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/filter/applyBottomItemsFilter`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action applyBottomPercentFilter
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function applyBottomPctFilter(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, ApplyBottomPctFilterRequest payload, ApplyBottomPctFilterHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/filter/applyBottomPercentFilter`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action applyCellColorFilter
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function applyCellColorFilter(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, ApplyCellColorFilterRequest payload, ApplyCellColorFilterHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/filter/applyCellColorFilter`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action applyCustomFilter
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function applyCustomFilter(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, ApplyCustomFilterRequest payload, ApplyCustomFilterHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/filter/applyCustomFilter`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action applyDynamicFilter
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function applyDynamicFilter(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, ApplyDynamicFilterRequest payload, ApplyDynamicFilterHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/filter/applyDynamicFilter`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action applyFontColorFilter
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function applyFontColorFilter(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, ApplyCellColorFilterRequest payload, ApplyFontColorFilterHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/filter/applyFontColorFilter`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action applyIconFilter
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function applyIconFilter(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, ApplyIconFilterRequest payload, ApplyIconFilterHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/filter/applyIconFilter`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action applyTopItemsFilter
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function applyTopItemsFilter(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, ApplyBottomItemsFilterRequest payload, ApplyTopItemsFilterHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/filter/applyTopItemsFilter`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action applyTopPercentFilter
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function applyTopPctFilter(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, ApplyBottomPctFilterRequest payload, ApplyTopPctFilterHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/filter/applyTopPercentFilter`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action applyValuesFilter
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function applyValuesFilter(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, ApplyValuesFilterRequest payload, ApplyValuesFilterHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/filter/applyValuesFilter`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action clear
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function clearColumnFilter(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, ClearColumnFilterHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/filter/clear`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke function dataBodyRange
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + return - The retrieved column data body range 
    remote isolated function getColumnDataBodyRange(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, GetColumnDataBodyRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/dataBodyRange()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke function headerRowRange
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + return - The retrieved column header row range 
    remote isolated function getColumnHeaderRowRange(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, GetColumnHeaderRowRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/headerRowRange()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke function range
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + return - The retrieved column range 
    remote isolated function getColumnRange(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, GetColumnRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/range()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke function totalRowRange
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableColumnId - The unique identifier of workbookTableColumn
    # + headers - Headers to be sent with the request 
    # + return - The retrieved column total row range 
    remote isolated function getColumnTotalRowRange(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableColumnId, GetColumnTotalRowRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/${getEncodedUri(workbookTableColumnId)}/totalRowRange()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke action add
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - The created table column 
    remote isolated function addColumn(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, AddColumnRequest payload, AddColumnHeaders headers = {}) returns AddColumnResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/add`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke function count
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + return - The number of table columns 
    remote isolated function countColumns(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, CountColumnsHeaders headers = {}) returns CountResult|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/columns/count()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke action clearFilters
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function clearTableFilters(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, ClearTableFiltersHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/clearFilters`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action convertToRange
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + return - The range the table was converted into 
    remote isolated function convertTableToRange(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, ConvertTableToRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/convertToRange`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke function dataBodyRange
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved table data body range 
    remote isolated function getTableDataBodyRange(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, GetTableDataBodyRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/dataBodyRange()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke function headerRowRange
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved table header row range 
    remote isolated function getTableHeaderRowRange(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, GetTableHeaderRowRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/headerRowRange()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke function range
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved table range 
    remote isolated function getTableRange(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, GetTableRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/range()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke action reapplyFilters
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function reapplyTableFilters(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, ReapplyTableFiltersHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/reapplyFilters`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke function totalRowRange
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + return - The retrieved table total row range 
    remote isolated function getTableTotalRowRange(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, GetTableTotalRowRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/totalRowRange()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get rows from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Retrieved collection 
    remote isolated function listRows(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, ListRowsHeaders headers = {}, *ListRowsQueries queries) returns TableRowCollectionResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/rows`;
        map<Encoding> queryParamEncoding = {"$orderby": {style: FORM, explode: false}, "$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Create table row
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + payload - The table row to create 
    # + return - The created table row 
    remote isolated function createRow(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, TableRow payload, CreateRowHeaders headers = {}) returns TableRow|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/rows`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get rows from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableRowId - The unique identifier of workbookTableRow
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved table row 
    remote isolated function getRow(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableRowId, GetRowHeaders headers = {}, *GetRowQueries queries) returns TableRow|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/rows/${getEncodedUri(workbookTableRowId)}`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Delete table row
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableRowId - The unique identifier of workbookTableRow
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function deleteRow(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableRowId, DeleteRowHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/rows/${getEncodedUri(workbookTableRowId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update table row
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableRowId - The unique identifier of workbookTableRow
    # + headers - Headers to be sent with the request 
    # + payload - The table row properties to update 
    # + return - The updated table row 
    remote isolated function updateRow(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableRowId, TableRow payload, UpdateRowHeaders headers = {}) returns TableRow|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/rows/${getEncodedUri(workbookTableRowId)}`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke function range
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + workbookTableRowId - The unique identifier of workbookTableRow
    # + headers - Headers to be sent with the request 
    # + return - The retrieved table row range 
    remote isolated function getTableRowRange(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, string workbookTableRowId, GetTableRowRangeHeaders headers = {}) returns RangeResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/rows/${getEncodedUri(workbookTableRowId)}/range()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke action add
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - The created table row 
    remote isolated function addRow(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, AddRowRequest payload, AddRowHeaders headers = {}) returns TableRowOperationResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/rows/add`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke function count
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + return - The number of table rows 
    remote isolated function countRows(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, CountRowsHeaders headers = {}) returns CountResult|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/rows/count()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get sort from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved table sort 
    remote isolated function getTableSort(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, GetTableSortHeaders headers = {}, *GetTableSortQueries queries) returns TableSort|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/sort`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Update table sort
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + payload - The table sort properties to update 
    # + return - The updated table sort 
    remote isolated function updateTableSort(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, TableSort payload, UpdateTableSortHeaders headers = {}) returns TableSort|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/sort`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # Invoke action apply
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - Success 
    remote isolated function applyTableSort(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, ApplyTableSortRequest payload, ApplyTableSortHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/sort/apply`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action clear
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function clearTableSort(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, ClearTableSortHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/sort/clear`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke action reapply
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + return - Success 
    remote isolated function reapplyTableSort(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, ReapplyTableSortHeaders headers = {}) returns error? {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/sort/reapply`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Get worksheet from drives
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + workbookTableId - The unique identifier of workbookTable
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The retrieved worksheet 
    remote isolated function getTableWorksheet(string driveId, string driveItemId, string workbookWorksheetId, string workbookTableId, GetTableWorksheetHeaders headers = {}, *GetTableWorksheetQueries queries) returns Worksheet|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/${getEncodedUri(workbookTableId)}/worksheet`;
        map<Encoding> queryParamEncoding = {"$select": {style: FORM, explode: false}, "$expand": {style: FORM, explode: false}};
        resourcePath = resourcePath + check getPathForQueryParam(queries, queryParamEncoding);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke action add
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - The created worksheet table 
    remote isolated function addWorksheetTable(string driveId, string driveItemId, string workbookWorksheetId, AddTableRequest payload, AddWorksheetTableHeaders headers = {}) returns AddTableResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/add`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }

    # Invoke function count
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + workbookWorksheetId - The unique identifier of workbookWorksheet
    # + headers - Headers to be sent with the request 
    # + return - The number of worksheet tables 
    remote isolated function countWorksheetTables(string driveId, string driveItemId, string workbookWorksheetId, CountWorksheetTablesHeaders headers = {}) returns CountResult|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/${getEncodedUri(workbookWorksheetId)}/tables/count()`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Get the number of the resource
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - The count of the resource 
    remote isolated function countWorksheets(string driveId, string driveItemId, CountWorksheetsHeaders headers = {}, *CountWorksheetsQueries queries) returns string|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/$count`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->get(resourcePath, httpHeaders);
    }

    # Invoke action add
    #
    # + driveId - The unique identifier of drive
    # + driveItemId - The unique identifier of driveItem
    # + headers - Headers to be sent with the request 
    # + payload - Action parameters 
    # + return - The created worksheet 
    remote isolated function addWorksheet(string driveId, string driveItemId, AddWorksheetRequest payload, AddWorksheetHeaders headers = {}) returns AddWorksheetResponse|error {
        string resourcePath = string `/drives/${getEncodedUri(driveId)}/items/${getEncodedUri(driveItemId)}/workbook/worksheets/add`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, httpHeaders);
    }
}
