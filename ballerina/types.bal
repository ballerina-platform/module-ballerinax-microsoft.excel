// AUTO-GENERATED FILE. DO NOT MODIFY.
// This file is auto-generated by the Ballerina OpenAPI tool.

// Copyright (c) 2026, WSO2 LLC. (http://www.wso2.com).
//
// WSO2 LLC. licenses this file to you under the Apache License,
// Version 2.0 (the "License"); you may not use this file except
// in compliance with the License.
// You may obtain a copy of the License at
//
// http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.

import ballerina/constraint;
import ballerina/data.jsondata;
import ballerina/http;

public type Entity record {
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The unique identifier for an entity. Read-only
    string id?;
};

# Represents the Queries record for the operation: getSeriesAxis
public type GetSeriesAxisQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Queries record for the operation: getWorksheetNamedItemWorksheet
public type GetWorksheetNamedItemWorksheetQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: getValueAxis
public type GetValueAxisHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type TableColumnCollectionResponse record {
    *BaseCollectionPaginationCountResponse;
    TableColumn[] value?;
};

# Represents the Headers record for the operation: getChartAxes
public type GetChartAxesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: countWorksheetNamedItems
public type CountWorksheetNamedItemsQueries record {
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
};

# Represents the Headers record for the operation: createTable
public type CreateTableHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateWorksheet
public type UpdateWorksheetHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getApplication
public type GetApplicationHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ChartPoint record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The format properties of the chart point. Read-only
    ChartPointFormat|record {} format?;
    # The value of a chart point. Read-only
    anydata value?;
};

# Represents the Headers record for the operation: updateOperation
public type UpdateOperationHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getTableRowOperationResult
public type GetTableRowOperationResultHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateDataLabels
public type UpdateDataLabelsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Provides a set of configurations for controlling the behaviours when communicating with a remote HTTP endpoint.
@display {label: "Connection Config"}
public type ConnectionConfig record {|
    # Configurations related to client authentication
    http:BearerTokenConfig|OAuth2RefreshTokenGrantConfig auth;
    # The HTTP version understood by the client
    http:HttpVersion httpVersion = http:HTTP_2_0;
    # Configurations related to HTTP/1.x protocol
    http:ClientHttp1Settings http1Settings = {};
    # Configurations related to HTTP/2 protocol
    http:ClientHttp2Settings http2Settings = {};
    # The maximum time to wait (in seconds) for a response before closing the connection
    decimal timeout = 30;
    # The choice of setting `forwarded`/`x-forwarded` header
    string forwarded = "disable";
    # Configurations associated with Redirection
    http:FollowRedirects followRedirects?;
    # Configurations associated with request pooling
    http:PoolConfiguration poolConfig?;
    # HTTP caching related configurations
    http:CacheConfig cache = {};
    # Specifies the way of handling compression (`accept-encoding`) header
    http:Compression compression = http:COMPRESSION_AUTO;
    # Configurations associated with the behaviour of the Circuit Breaker
    http:CircuitBreakerConfig circuitBreaker?;
    # Configurations associated with retrying
    http:RetryConfig retryConfig?;
    # Configurations associated with cookies
    http:CookieConfig cookieConfig?;
    # Configurations associated with inbound response size limits
    http:ResponseLimitConfigs responseLimits = {};
    # SSL/TLS-related options
    http:ClientSecureSocket secureSocket?;
    # Proxy server related options
    http:ProxyConfig proxy?;
    # Provides settings related to client socket configuration
    http:ClientSocketConfig socketConfig = {};
    # Enables the inbound payload validation functionality which provided by the constraint package. Enabled by default
    boolean validation = true;
    # Enables relaxed data binding on the client side. When enabled, `nil` values are treated as optional, 
    # and absent fields are handled as `nilable` types. Enabled by default.
    boolean laxDataBinding = true;
|};

# Represents the Queries record for the operation: listNamedItems
public type ListNamedItemsQueries record {
    # Skip the first n items
    @http:Query {name: "$skip"}
    int dollarSkip?;
    # Show only the first n items
    @http:Query {name: "$top"}
    int dollarTop?;
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
    # Order items by property values
    @http:Query {name: "$orderby"}
    string[] dollarOrderby?;
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Include count of items
    @http:Query {name: "$count"}
    boolean dollarCount?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Queries record for the operation: listOperations
public type ListOperationsQueries record {
    # Skip the first n items
    @http:Query {name: "$skip"}
    int dollarSkip?;
    # Show only the first n items
    @http:Query {name: "$top"}
    int dollarTop?;
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
    # Order items by property values
    @http:Query {name: "$orderby"}
    string[] dollarOrderby?;
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Include count of items
    @http:Query {name: "$count"}
    boolean dollarCount?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: getColumnFilter
public type GetColumnFilterHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: mergeUsedRange
public type MergeUsedRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getWorkbook
public type GetWorkbookHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getUsedRangeSort
public type GetUsedRangeSortQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: listOperations
public type ListOperationsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ChartTitleFormat record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents the fill format of an object, which includes background formatting information. Read-only
    ChartFill|record {} fill?;
    # Represents the font attributes (font name, font size, color, etc.) for the current object. Read-only
    ChartFont|record {} font?;
};

# Represents the Headers record for the operation: updateProtection
public type UpdateProtectionHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getChartImageFitted
public type GetChartImageFittedHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getWorkbook
public type GetWorkbookQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type CountResult record {
    @constraint:Number {minValue: -2147483648, maxValue: 2147483647}
    decimal value?;
};

public type SetChartPositionRequest record {
    anydata endCell?;
    anydata startCell?;
};

public type ApplyValuesFilterRequest record {
    anydata values?;
};

# Represents the Headers record for the operation: getColumnRange
public type GetColumnRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: createWorksheetNamedItem
public type CreateWorksheetNamedItemHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getRangeFormat
public type GetRangeFormatQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type ApplyBottomPctFilterRequest record {
    @constraint:Number {minValue: -2147483648, maxValue: 2147483647}
    decimal percent?;
};

# Represents the Queries record for the operation: getRow
public type GetRowQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: updateChartFormat
public type UpdateChartFormatHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateColumn
public type UpdateColumnHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type SessionInfoResponseEmpty record {
};

# Represents the Queries record for the operation: listComments
public type ListCommentsQueries record {
    # Skip the first n items
    @http:Query {name: "$skip"}
    int dollarSkip?;
    # Show only the first n items
    @http:Query {name: "$top"}
    int dollarTop?;
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
    # Order items by property values
    @http:Query {name: "$orderby"}
    string[] dollarOrderby?;
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Include count of items
    @http:Query {name: "$count"}
    boolean dollarCount?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type AddChartRequest record {
    string seriesBy?;
    string 'type?;
    anydata sourceData?;
};

# Represents the Queries record for the operation: getRangeByAddressFormat
public type GetRangeByAddressFormatQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: closeSession
public type CloseSessionHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: listWorksheets
public type ListWorksheetsQueries record {
    # Skip the first n items
    @http:Query {name: "$skip"}
    int dollarSkip?;
    # Show only the first n items
    @http:Query {name: "$top"}
    int dollarTop?;
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
    # Order items by property values
    @http:Query {name: "$orderby"}
    string[] dollarOrderby?;
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Include count of items
    @http:Query {name: "$count"}
    boolean dollarCount?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: deleteRangeByAddress
public type DeleteRangeByAddressHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: listComments
public type ListCommentsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type AddNamedItemRequest record {
    anydata reference?;
    string? name?;
    string? comment?;
};

# Represents the Headers record for the operation: updateChart
public type UpdateChartHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getRangeSort
public type GetRangeSortHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getTableSort
public type GetTableSortQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: updateComment
public type UpdateCommentHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getRangeSort
public type GetRangeSortQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: countWorksheetNamedItems
public type CountWorksheetNamedItemsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type SessionInfo record {
    # true for persistent session. false for non-persistent session (view mode)
    boolean? persistChanges?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # ID of the workbook session
    string? id?;
};

public type ChartSeries record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The name of a series in a chart
    string? name?;
    # The formatting of a chart series, which includes fill and line formatting. Read-only
    ChartSeriesFormat|record {} format?;
    # A collection of all points in the series. Read-only
    ChartPoint[] points?;
};

public type SetChartDataRequest record {
    string seriesBy?;
    anydata sourceData?;
};

# Represents the Headers record for the operation: getSeriesAxis
public type GetSeriesAxisHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getApplication
public type GetApplicationQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: countWorksheets
public type CountWorksheetsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateNamedItem
public type UpdateNamedItemHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getPivotTableWorksheet
public type GetPivotTableWorksheetQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type OperationStatus "notStarted"|"running"|"succeeded"|"failed";

public type NamedItemResponseEmpty record {
};

# Represents the Headers record for the operation: clearRange
public type ClearRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: reapplyTableSort
public type ReapplyTableSortHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ProtectWorksheetRequest record {
    WorksheetProtectionOptions|record {} options?;
};

public type Chart record {
    *Entity;
    # Represents the legend for the chart. Read-only
    ChartLegend|record {} legend?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents chart axes. Read-only
    ChartAxes|record {} axes?;
    # Encapsulates the format properties for the chart area. Read-only
    ChartAreaFormat|record {} format?;
    # Represents the title of the specified chart, including the text, visibility, position and formatting of the title. Read-only
    ChartTitle|record {} title?;
    # Represents the data labels on the chart. Read-only
    ChartDataLabels|record {} dataLabels?;
    # Represents the distance, in points, from the top edge of the object to the top of row 1 (on a worksheet) or the top of the chart area (on a chart)
    decimal|string|ReferenceNumeric? top?;
    # The distance, in points, from the left side of the chart to the worksheet origin
    decimal|string|ReferenceNumeric? left?;
    # Represents either a single series or collection of series in the chart. Read-only
    ChartSeries[] series?;
    # Represents the name of a chart object
    string? name?;
    # Represents the width, in points, of the chart object
    decimal|string|ReferenceNumeric? width?;
    # The worksheet containing the current chart. Read-only
    Worksheet|record {} worksheet?;
    # Represents the height, in points, of the chart object
    decimal|string|ReferenceNumeric? height?;
};

# Represents the Headers record for the operation: applyIconFilter
public type ApplyIconFilterHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getTableDataBodyRange
public type GetTableDataBodyRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: listRows
public type ListRowsQueries record {
    # Skip the first n items
    @http:Query {name: "$skip"}
    int dollarSkip?;
    # Show only the first n items
    @http:Query {name: "$top"}
    int dollarTop?;
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
    # Order items by property values
    @http:Query {name: "$orderby"}
    string[] dollarOrderby?;
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Include count of items
    @http:Query {name: "$count"}
    boolean dollarCount?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: insertRangeByAddress
public type InsertRangeByAddressHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getTableWorksheet
public type GetTableWorksheetHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type Functions record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
};

# Represents the Headers record for the operation: updateChartSeries
public type UpdateChartSeriesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getWorksheetNamedItemRange
public type GetWorksheetNamedItemRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: unmergeUsedRange
public type UnmergeUsedRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type Workbook record {
    *Entity;
    # Represents a collection of tables associated with the workbook. Read-only
    Table[] tables?;
    # Represents a collection of comments in a workbook
    Comment[] comments?;
    # Represents a collection of workbooks scoped named items (named ranges and constants). Read-only
    NamedItem[] names?;
    # The status of workbook operations. Getting an operation collection is not supported, but you can get the status of a long-running operation if the Location header is returned in the response. Read-only
    Operation[] operations?;
    # Represents a collection of worksheets associated with the workbook. Read-only
    Worksheet[] worksheets?;
    Application|record {} application?;
    Functions|record {} functions?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
};

public type OperationError record {
    # The error code
    string? code?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    OperationError|record {} innerError?;
    # The error message
    string? message?;
};

# Represents the Headers record for the operation: deleteNamedItem
public type DeleteNamedItemHeaders record {
    # ETag
    @http:Header {name: "If-Match"}
    string ifMatch?;
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: clearTableSort
public type ClearTableSortHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type TableRowOperationResponseEmpty record {
};

public type AddColumnRequest record {
    anydata values?;
    string? name?;
    decimal? index?;
};

public type ChartDataLabels record {
    *Entity;
    # Boolean value that represents whether the data label value is visible
    boolean? showValue?;
    # Boolean value that represents whether the data label bubble size is visible
    boolean? showBubbleSize?;
    # Boolean value that represents whether the data label legend key is visible
    boolean? showLegendKey?;
    # Boolean value that represents whether the data label percentage is visible
    boolean? showPercentage?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Boolean value that represents whether the data label series name is visible
    boolean? showSeriesName?;
    # Represents the format of chart data labels, which includes fill and font formatting. Read-only
    ChartDataLabelFormat|record {} format?;
    # Boolean value that represents whether the data label category name is visible
    boolean? showCategoryName?;
    # DataLabelPosition value that represents the position of the data label. The possible values are: None, Center, InsideEnd, InsideBase, OutsideEnd, Left, Right, Top, Bottom, BestFit, Callout
    string? position?;
    # String that represents the separator used for the data labels on a chart
    string? separator?;
};

# Represents the Queries record for the operation: getCategoryAxis
public type GetCategoryAxisQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type AddWorksheetRequest record {
    string? name?;
};

public type RangeBorder record {
    *Entity;
    # Indicates the specific side of the border. The possible values are: EdgeTop, EdgeBottom, EdgeLeft, EdgeRight, InsideVertical, InsideHorizontal, DiagonalDown, DiagonalUp. Read-only
    string? sideIndex?;
    # The HTML color code that represents the color of the border line. Can either be of the form #RRGGBB, for example 'FFA500', or a named HTML color, for example 'orange'
    string? color?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The weight of the border around a range. The possible values are: Hairline, Thin, Medium, Thick
    string? weight?;
    # Indicates the line style for the border. The possible values are: None, Continuous, Dash, DashDot, DashDotDot, Dot, Double, SlantDashDot
    string? style?;
};

# Represents the Headers record for the operation: getColumnTotalRowRange
public type GetColumnTotalRowRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: listReplies
public type ListRepliesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getReply
public type GetReplyHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: applyDynamicFilter
public type ApplyDynamicFilterHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type WorksheetProtection record {
    *Entity;
    # Indicates whether the worksheet is protected.  Read-only
    boolean protected?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Worksheet protection options. Read-only
    WorksheetProtectionOptions|record {} options?;
};

# Represents the Queries record for the operation: listColumns
public type ListColumnsQueries record {
    # Skip the first n items
    @http:Query {name: "$skip"}
    int dollarSkip?;
    # Show only the first n items
    @http:Query {name: "$top"}
    int dollarTop?;
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
    # Order items by property values
    @http:Query {name: "$orderby"}
    string[] dollarOrderby?;
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Include count of items
    @http:Query {name: "$count"}
    boolean dollarCount?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type ChartFont record {
    *Entity;
    # The HTML color code representation of the text color. For example #FF0000 represents Red
    string? color?;
    # The size of the font. For example,  11
    decimal|string|ReferenceNumeric? size?;
    # The type of underlining applied to the font. The possible values are: None, Single
    string? underline?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The font name. For example 'Calibri'
    string? name?;
    # Indicates whether the fond is bold
    boolean? bold?;
    # Indicates whether the fond is italic
    boolean? italic?;
};

public type MergeRangeRequest record {
    boolean across = false;
};

# Represents the Headers record for the operation: reapplyTableFilters
public type ReapplyTableFiltersHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: deleteWorksheetNamedItem
public type DeleteWorksheetNamedItemHeaders record {
    # ETag
    @http:Header {name: "If-Match"}
    string ifMatch?;
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: deleteComment
public type DeleteCommentHeaders record {
    # ETag
    @http:Header {name: "If-Match"}
    string ifMatch?;
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateReply
public type UpdateReplyHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateRangeByAddressFormat
public type UpdateRangeByAddressFormatHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type TableSort record {
    *Entity;
    # Indicates whether the casing impacted the last sort of the table. Read-only
    boolean matchCase?;
    # The Chinese character ordering method last used to sort the table. The possible values are: PinYin, StrokeCount. Read-only
    string method?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The list of the current conditions last used to sort the table. Read-only
    SortField[] fields?;
};

public type SortField record {
    # Represents the type of sorting of this condition. The possible values are: Value, CellColor, FontColor, Icon
    string sortOn?;
    # Represents the color that is the target of the condition if the sorting is on font or cell color
    string? color?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents the icon that is the target of the condition if the sorting is on the cell's icon
    Icon|record {} icon?;
    # Represents whether the sorting is done in an ascending fashion
    boolean 'ascending?;
    # Represents additional sorting options for this field. The possible values are: Normal, TextAsNumber
    string dataOption?;
    # Represents the column (or row, depending on the sort orientation) that the condition is on. Represented as an offset from the first column (or row)
    @constraint:Number {minValue: -2147483648, maxValue: 2147483647}
    decimal 'key?;
};

public type AddNamedItemFormulaLocalRequest record {
    string? name?;
    string? formula?;
    string? comment?;
};

public type AddWorksheetResponseEmpty record {
};

# Represents the Headers record for the operation: addWorksheetTable
public type AddWorksheetTableHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: createPivotTable
public type CreatePivotTableHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getChartSeries
public type GetChartSeriesQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: deleteColumn
public type DeleteColumnHeaders record {
    # ETag
    @http:Header {name: "If-Match"}
    string ifMatch?;
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getChartTitle
public type GetChartTitleHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getPivotTableWorksheet
public type GetPivotTableWorksheetHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getPivotTable
public type GetPivotTableHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: deleteUsedRange
public type DeleteUsedRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateWorkbook
public type UpdateWorkbookHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: clearColumnFilter
public type ClearColumnFilterHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updatePivotTable
public type UpdatePivotTableHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: createReply
public type CreateReplyHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: applyTableSort
public type ApplyTableSortHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: deletePivotTable
public type DeletePivotTableHeaders record {
    # ETag
    @http:Header {name: "If-Match"}
    string ifMatch?;
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: refreshPivotTable
public type RefreshPivotTableHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: listWorksheetTables
public type ListWorksheetTablesQueries record {
    # Skip the first n items
    @http:Query {name: "$skip"}
    int dollarSkip?;
    # Show only the first n items
    @http:Query {name: "$top"}
    int dollarTop?;
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
    # Order items by property values
    @http:Query {name: "$orderby"}
    string[] dollarOrderby?;
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Include count of items
    @http:Query {name: "$count"}
    boolean dollarCount?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type BaseCollectionPaginationCountResponse record {
    @jsondata:Name {value: "@odata.nextLink"}
    string? atOdataNextLink?;
    @jsondata:Name {value: "@odata.count"}
    int? atOdataCount?;
};

public type ClearRangeRequest record {
    string applyTo?;
};

# Represents the Headers record for the operation: getCell
public type GetCellHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getRow
public type GetRowHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ChartSeriesFormat record {
    *Entity;
    # Represents line formatting. Read-only
    ChartLineFormat|record {} line?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents the fill format of a chart series, which includes background formatting information. Read-only
    ChartFill|record {} fill?;
};

# Represents the Headers record for the operation: deleteWorksheet
public type DeleteWorksheetHeaders record {
    # ETag
    @http:Header {name: "If-Match"}
    string ifMatch?;
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ChartLegendFormat record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents the fill format of an object, which includes background formating information. Read-only
    ChartFill|record {} fill?;
    # Represents the font attributes such as font name, font size, color, etc. of a chart legend. Read-only
    ChartFont|record {} font?;
};

# Represents the Headers record for the operation: listWorksheets
public type ListWorksheetsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type FormatProtection record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Indicates whether Excel locks the cells in the object. A null value indicates that the entire range doesn't have uniform lock setting
    boolean? locked?;
    # Indicates whether Excel hides the formula for the cells in the range. A null value indicates that the entire range doesn't have uniform formula hidden setting
    boolean? formulaHidden?;
};

# Represents the Headers record for the operation: updateLegend
public type UpdateLegendHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type Icon record {
    # The set that the icon is part of. The possible values are: Invalid, ThreeArrows, ThreeArrowsGray, ThreeFlags, ThreeTrafficLights1, ThreeTrafficLights2, ThreeSigns, ThreeSymbols, ThreeSymbols2, FourArrows, FourArrowsGray, FourRedToBlack, FourRating, FourTrafficLights, FiveArrows, FiveArrowsGray, FiveRating, FiveQuarters, ThreeStars, ThreeTriangles, FiveBoxes
    string set?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The index of the icon in the given set
    @constraint:Number {minValue: -2147483648, maxValue: 2147483647}
    decimal index?;
};

# Represents the Headers record for the operation: getRangeFormat
public type GetRangeFormatHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: applyValuesFilter
public type ApplyValuesFilterHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateRangeByAddressSort
public type UpdateRangeByAddressSortHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type RangeResponseEmpty record {
};

# Represents the Headers record for the operation: getProtection
public type GetProtectionHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: applyBottomItemsFilter
public type ApplyBottomItemsFilterHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getTableHeaderRowRange
public type GetTableHeaderRowRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getRangeByAddressFormat
public type GetRangeByAddressFormatHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: listPivotTables
public type ListPivotTablesQueries record {
    # Skip the first n items
    @http:Query {name: "$skip"}
    int dollarSkip?;
    # Show only the first n items
    @http:Query {name: "$top"}
    int dollarTop?;
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
    # Order items by property values
    @http:Query {name: "$orderby"}
    string[] dollarOrderby?;
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Include count of items
    @http:Query {name: "$count"}
    boolean dollarCount?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: getWorksheetNamedItemWorksheet
public type GetWorksheetNamedItemWorksheetHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type AddChartResponseEmpty record {
};

public type ChartLegend record {
    *Entity;
    # Indicates whether the chart legend is visible
    boolean visible?;
    # Indicates whether the chart legend should overlap with the main body of the chart
    boolean? overlay?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents the formatting of a chart legend, which includes fill and font formatting. Read-only
    ChartLegendFormat|record {} format?;
    # Represents the position of the legend on the chart. The possible values are: Top, Bottom, Left, Right, Corner, Custom
    string? position?;
};

# Represents the Headers record for the operation: createWorksheet
public type CreateWorksheetHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: applyFontColorFilter
public type ApplyFontColorFilterHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type TableRowOperationResponse TableRow|TableRowOperationResponseEmpty?;

# Represents the Headers record for the operation: addRow
public type AddRowHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: applyBottomPctFilter
public type ApplyBottomPctFilterHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type AddTableRequest record {
    string? address?;
    boolean hasHeaders = false;
};

# Represents the Headers record for the operation: mergeRangeByAddress
public type MergeRangeByAddressHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type RangeResponse Range|RangeResponseEmpty?;

# Represents the Headers record for the operation: getChartSeries
public type GetChartSeriesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: applyColumnFilter
public type ApplyColumnFilterHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: countRows
public type CountRowsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: deleteRange
public type DeleteRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getUsedRangeFormat
public type GetUsedRangeFormatHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# OAuth2 Refresh Token Grant Configs
public type OAuth2RefreshTokenGrantConfig record {|
    *http:OAuth2RefreshTokenGrantConfig;
    # Refresh URL
    string refreshUrl = "https://login.microsoftonline.com/common/oauth2/v2.0/token";
|};

# Represents the Queries record for the operation: countComments
public type CountCommentsQueries record {
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
};

# Represents the Queries record for the operation: getValueAxis
public type GetValueAxisQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: listNamedItems
public type ListNamedItemsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getNamedItemWorksheet
public type GetNamedItemWorksheetQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Queries record for the operation: getChartTitle
public type GetChartTitleQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type RangeFill record {
    *Entity;
    # HTML color code representing the color of the border line. Can either be of the form #RRGGBB, for example 'FFA500', or be a named HTML color, for example 'orange'
    string? color?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
};

public type ChartAxisTitleFormat record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents the font attributes, such as font name, font size, color, etc. of chart axis title object. Read-only
    ChartFont|record {} font?;
};

# Represents the Headers record for the operation: countColumns
public type CountColumnsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: countOperations
public type CountOperationsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ReferenceNumeric "-INF"|"INF"|"NaN"?;

# Represents the Headers record for the operation: convertTableToRange
public type ConvertTableToRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getWorksheet
public type GetWorksheetQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: insertRange
public type InsertRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: listColumns
public type ListColumnsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: listReplies
public type ListRepliesQueries record {
    # Skip the first n items
    @http:Query {name: "$skip"}
    int dollarSkip?;
    # Show only the first n items
    @http:Query {name: "$top"}
    int dollarTop?;
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
    # Order items by property values
    @http:Query {name: "$orderby"}
    string[] dollarOrderby?;
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Include count of items
    @http:Query {name: "$count"}
    boolean dollarCount?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type Filter record {
    *Entity;
    # The currently applied filter on the given column. Read-only
    FilterCriteria|record {} criteria?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
};

# Represents the Headers record for the operation: applyCustomFilter
public type ApplyCustomFilterHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: listChartSeries
public type ListChartSeriesQueries record {
    # Skip the first n items
    @http:Query {name: "$skip"}
    int dollarSkip?;
    # Show only the first n items
    @http:Query {name: "$top"}
    int dollarTop?;
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
    # Order items by property values
    @http:Query {name: "$orderby"}
    string[] dollarOrderby?;
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Include count of items
    @http:Query {name: "$count"}
    boolean dollarCount?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type NamedItem record {
    *Entity;
    # Indicates whether the object is visible
    boolean visible?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Indicates whether the name is scoped to the workbook or to a specific worksheet. Read-only
    string scope?;
    # The name of the object. Read-only
    string? name?;
    # The comment associated with this name
    string? comment?;
    # Returns the worksheet to which the named item is scoped. Available only if the item is scoped to the worksheet. Read-only
    Worksheet|record {} worksheet?;
    # The type of reference is associated with the name. The possible values are: String, Integer, Double, Boolean, Range. Read-only
    string? 'type?;
    # The formula that the name is defined to refer to. For example, =Sheet14!$B$2:$H$12 and =4.75. Read-only
    anydata value?;
};

# Represents the Queries record for the operation: getReply
public type GetReplyQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type RangeSort record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
};

# Represents the Headers record for the operation: getNamedItemWorksheet
public type GetNamedItemWorksheetHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: countReplies
public type CountRepliesQueries record {
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
};

# Represents the Headers record for the operation: unprotectWorksheet
public type UnprotectWorksheetHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getComment
public type GetCommentHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ChartAxisTitle record {
    *Entity;
    # A Boolean that specifies the visibility of an axis title
    boolean visible?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents the formatting of chart axis title. Read-only
    ChartAxisTitleFormat|record {} format?;
    # Represents the axis title
    string? text?;
};

# Represents the Headers record for the operation: getChart
public type GetChartHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateWorksheetNamedItem
public type UpdateWorksheetNamedItemHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getRange
public type GetRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: addChart
public type AddChartHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateRangeFormat
public type UpdateRangeFormatHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: addColumn
public type AddColumnHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: clearUsedRange
public type ClearUsedRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getProtection
public type GetProtectionQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type TableRowCollectionResponse record {
    *BaseCollectionPaginationCountResponse;
    TableRow[] value?;
};

# Represents the Headers record for the operation: getSessionInfo
public type GetSessionInfoHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: createChart
public type CreateChartHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getDataLabels
public type GetDataLabelsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getChartWorksheet
public type GetChartWorksheetQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: addWorksheetNamedItemFormulaLocal
public type AddWorksheetNamedItemFormulaLocalHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateUsedRangeSort
public type UpdateUsedRangeSortHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: applyTopItemsFilter
public type ApplyTopItemsFilterHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: listWorksheetNamedItems
public type ListWorksheetNamedItemsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: refreshSession
public type RefreshSessionHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getChartImage
public type GetChartImageHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type AddWorksheetResponse Worksheet|AddWorksheetResponseEmpty?;

# Represents the Headers record for the operation: countCharts
public type CountChartsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type SessionInfoResponse SessionInfo|SessionInfoResponseEmpty?;

# Represents the Queries record for the operation: getLegend
public type GetLegendQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Queries record for the operation: getWorksheetNamedItem
public type GetWorksheetNamedItemQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: listWorksheetTables
public type ListWorksheetTablesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getTableRowRange
public type GetTableRowRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: addNamedItem
public type AddNamedItemHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type TableColumn record {
    *Entity;
    # The filter applied to the column. Read-only
    Filter|record {} filter?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # TRepresents the raw values of the specified range. The data returned could be of type string, number, or a Boolean. Cell that contain an error will return the error string
    anydata values?;
    # The name of the table column
    string? name?;
    # The index of the column within the columns collection of the table. Zero-indexed. Read-only
    decimal index?;
};

public type CommentCollectionResponse record {
    *BaseCollectionPaginationCountResponse;
    Comment[] value?;
};

# Represents the Queries record for the operation: getOperation
public type GetOperationQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type OperationCollectionResponse record {
    *BaseCollectionPaginationCountResponse;
    Operation[] value?;
};

# Represents the Headers record for the operation: createColumn
public type CreateColumnHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type NamedItemResponse NamedItem|NamedItemResponseEmpty?;

# Represents the Headers record for the operation: updateSeriesAxis
public type UpdateSeriesAxisHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type TableRow record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The raw values of the specified range. The data returned could be of type string, number, or a Boolean. Any cell that contain an error will return the error string
    anydata values?;
    # The index of the row within the rows collection of the table. Zero-based. Read-only
    decimal index?;
};

# Represents the Headers record for the operation: refreshAllPivotTables
public type RefreshAllPivotTablesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type Application record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Returns the calculation mode used in the workbook. The possible values are: Automatic, AutomaticExceptTables, Manual
    string calculationMode?;
};

# Represents the Headers record for the operation: deleteWorksheetTable
public type DeleteWorksheetTableHeaders record {
    # ETag
    @http:Header {name: "If-Match"}
    string ifMatch?;
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: setChartPosition
public type SetChartPositionHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: applyCellColorFilter
public type ApplyCellColorFilterHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type WorksheetCollectionResponse record {
    *BaseCollectionPaginationCountResponse;
    Worksheet[] value?;
};

# Represents the Headers record for the operation: updateValueAxis
public type UpdateValueAxisHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type NamedItemCollectionResponse record {
    *BaseCollectionPaginationCountResponse;
    NamedItem[] value?;
};

# Represents the Headers record for the operation: updateChartAxes
public type UpdateChartAxesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getChart
public type GetChartQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: countReplies
public type CountRepliesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type Worksheet record {
    *Entity;
    # The list of charts that are part of the worksheet. Read-only
    Chart[] charts?;
    # The list of tables that are part of the worksheet. Read-only
    Table[] tables?;
    # The list of names that are associated with the worksheet. Read-only
    NamedItem[] names?;
    # The visibility of the worksheet. The possible values are: Visible, Hidden, VeryHidden
    string visibility?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The display name of the worksheet
    string? name?;
    # The sheet protection object for a worksheet. Read-only
    WorksheetProtection|record {} protection?;
    # The zero-based position of the worksheet within the workbook
    decimal position?;
    # The list of piot tables that are part of the worksheet
    PivotTable[] pivotTables?;
};

public type ChartLineFormat record {
    *Entity;
    # The HTML color code that represents the color of lines in the chart
    string? color?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
};

public type ChartGridlinesFormat record {
    *Entity;
    # Represents chart line formatting. Read-only
    ChartLineFormat|record {} line?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
};

# Represents the Queries record for the operation: getPivotTable
public type GetPivotTableQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Queries record for the operation: getChartFormat
public type GetChartFormatQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type CreateSessionRequest record {
    boolean persistChanges = false;
};

# Represents the Headers record for the operation: getWorksheet
public type GetWorksheetHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getDataLabels
public type GetDataLabelsQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: getChartWorksheet
public type GetChartWorksheetHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: listWorksheetNamedItems
public type ListWorksheetNamedItemsQueries record {
    # Skip the first n items
    @http:Query {name: "$skip"}
    int dollarSkip?;
    # Show only the first n items
    @http:Query {name: "$top"}
    int dollarTop?;
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
    # Order items by property values
    @http:Query {name: "$orderby"}
    string[] dollarOrderby?;
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Include count of items
    @http:Query {name: "$count"}
    boolean dollarCount?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: getLegend
public type GetLegendHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ChartDataLabelFormat record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents the fill format of the current chart data label. Read-only
    ChartFill|record {} fill?;
    # Represents the font attributes (font name, font size, color, etc.) for a chart data label. Read-only
    ChartFont|record {} font?;
};

# Represents the Headers record for the operation: updateChartTitle
public type UpdateChartTitleHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ApplyIconFilterRequest record {
    Icon|record {} icon?;
};

# Represents the Headers record for the operation: calculateApplication
public type CalculateApplicationHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: setChartData
public type SetChartDataHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: insertUsedRange
public type InsertUsedRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: deleteChart
public type DeleteChartHeaders record {
    # ETag
    @http:Header {name: "If-Match"}
    string ifMatch?;
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type AddTableResponseEmpty record {
};

public type ChartPointFormat record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents the fill format of a chart, which includes background formatting information. Read-only
    ChartFill|record {} fill?;
};

public type Table record {
    *Entity;
    # Indicates whether the first column contains special formatting
    boolean highlightFirstColumn?;
    # Indicates whether the header row is visible or not. This value can be set to show or remove the header row
    boolean showHeaders?;
    # The list of all the columns in the table. Read-only
    TableColumn[] columns?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Indicates whether the rows show banded formatting in which odd rows are highlighted differently from even ones to make reading the table easier
    boolean showBandedRows?;
    # The sorting for the table. Read-only
    TableSort|record {} sort?;
    # The list of all the rows in the table. Read-only
    TableRow[] rows?;
    # Indicates whether the last column contains special formatting
    boolean highlightLastColumn?;
    # Indicates whether the columns show banded formatting in which odd columns are highlighted differently from even ones to make reading the table easier
    boolean showBandedColumns?;
    # Indicates whether the total row is visible or not. This value can be set to show or remove the total row
    boolean showTotals?;
    # The name of the table
    string? name?;
    # Indicates whether the filter buttons are visible at the top of each column header. Setting this is only allowed if the table contains a header row
    boolean showFilterButton?;
    # A legacy identifier used in older Excel clients. The value of the identifier remains the same even when the table is renamed. This property should be interpreted as an opaque string value and shouldn't be parsed to any other type. Read-only
    string? legacyId?;
    # A constant value that represents the Table style. The possible values are: TableStyleLight1 through TableStyleLight21, TableStyleMedium1 through TableStyleMedium28, TableStyleStyleDark1 through TableStyleStyleDark11. A custom user-defined style present in the workbook can also be specified
    string? style?;
    # The worksheet containing the current table. Read-only
    Worksheet|record {} worksheet?;
};

# Represents the Queries record for the operation: countOperations
public type CountOperationsQueries record {
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
};

# Represents the Headers record for the operation: protectWorksheet
public type ProtectWorksheetHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getUsedRangeFormat
public type GetUsedRangeFormatQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type AddColumnResponseEmpty record {
};

public type ApplyDynamicFilterRequest record {
    string criteria?;
};

# Represents the Queries record for the operation: getRangeByAddressSort
public type GetRangeByAddressSortQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: updateWorksheetTable
public type UpdateWorksheetTableHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type CommentReplyCollectionResponse record {
    *BaseCollectionPaginationCountResponse;
    CommentReply[] value?;
};

# Represents the Headers record for the operation: createWorksheetTable
public type CreateWorksheetTableHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: countPivotTables
public type CountPivotTablesQueries record {
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
};

public type ChartGridlines record {
    *Entity;
    # Indicates whether the axis gridlines are visible
    boolean visible?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents the formatting of chart gridlines. Read-only
    ChartGridlinesFormat|record {} format?;
};

# Represents the Headers record for the operation: getColumnHeaderRowRange
public type GetColumnHeaderRowRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getTableRange
public type GetTableRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type TableCollectionResponse record {
    *BaseCollectionPaginationCountResponse;
    Table[] value?;
};

# Represents the Headers record for the operation: updateCategoryAxis
public type UpdateCategoryAxisHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type PivotTableCollectionResponse record {
    *BaseCollectionPaginationCountResponse;
    PivotTable[] value?;
};

# Represents the Headers record for the operation: addWorksheet
public type AddWorksheetHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ChartAxisFormat record {
    *Entity;
    # Represents chart line formatting. Read-only
    ChartLineFormat|record {} line?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents the font attributes (font name, font size, color, etc.) for a chart axis element. Read-only
    ChartFont|record {} font?;
};

# Represents the Headers record for the operation: listChartSeries
public type ListChartSeriesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getComment
public type GetCommentQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: deleteOperation
public type DeleteOperationHeaders record {
    # ETag
    @http:Header {name: "If-Match"}
    string ifMatch?;
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getChartImageByWidth
public type GetChartImageByWidthHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: addWorksheetNamedItem
public type AddWorksheetNamedItemHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: createNamedItem
public type CreateNamedItemHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type FilterCriteria record {
    # The color applied to the cell
    string? color?;
    # Indicates whether a filter is applied to a column
    string filterOn?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The values that appear in the cell
    anydata values?;
    # An icon applied to a cell via conditional formatting
    Icon|record {} icon?;
    # A custom criterion
    string? criterion2?;
    # A dynamic formula specified in a custom filter
    string dynamicCriteria?;
    # An operator in a cell; for example, =, >, <, <=, or <>
    string operator?;
    # A custom criterion
    string? criterion1?;
};

# Represents the Headers record for the operation: listPivotTables
public type ListPivotTablesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type CalculateApplicationRequest record {
    string calculationType?;
};

# Represents the Headers record for the operation: getNamedItem
public type GetNamedItemHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getRangeByAddress
public type GetRangeByAddressHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getColumn
public type GetColumnQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type CommentReply record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The content type for the reply
    string contentType?;
    # The content of the reply
    string? content?;
};

public type RangeFont record {
    *Entity;
    # The HTML color code representation of the text color. For example, #FF0000 represents the color red
    string? color?;
    # The font size
    decimal|string|ReferenceNumeric? size?;
    # The type of underlining applied to the font. The possible values are: None, Single, Double, SingleAccountant, DoubleAccountant
    string? underline?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The font name. For example, 'Calibri'
    string? name?;
    # Inidicates whether the font is bold
    boolean? bold?;
    # Inidicates whether the font is italic
    boolean? italic?;
};

# Represents the Headers record for the operation: clearTableFilters
public type ClearTableFiltersHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getWorksheetTable
public type GetWorksheetTableQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: getChartFormat
public type GetChartFormatHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: countComments
public type CountCommentsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: countWorksheets
public type CountWorksheetsQueries record {
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
};

# Represents the Headers record for the operation: getUsedRangeValuesOnly
public type GetUsedRangeValuesOnlyHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getNamedItemRange
public type GetNamedItemRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ChartSeriesCollectionResponse record {
    *BaseCollectionPaginationCountResponse;
    ChartSeries[] value?;
};

public type ChartTitle record {
    *Entity;
    # Indicates whether the chart title is visible
    boolean visible?;
    # Indicates whether the chart title will overlay the chart or not
    boolean? overlay?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The formatting of a chart title, which includes fill and font formatting. Read-only
    ChartTitleFormat|record {} format?;
    # The title text of the chart
    string? text?;
};

public type ApplyFilterRequest record {
    FilterCriteria|record {} criteria?;
};

# Represents the Headers record for the operation: getTableSort
public type GetTableSortHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ChartCollectionResponse record {
    *BaseCollectionPaginationCountResponse;
    Chart[] value?;
};

# Represents the Headers record for the operation: getChartImageBySize
public type GetChartImageBySizeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ApplyTableSortRequest record {
    boolean matchCase = false;
    string method?;
    SortField[] fields?;
};

# Represents the Headers record for the operation: countWorksheetTables
public type CountWorksheetTablesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ChartAreaFormat record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents the fill format of an object, which includes background formatting information. Read-only
    ChartFill|record {} fill?;
    # Represents the font attributes (font name, font size, color, etc.) for the current object. Read-only
    ChartFont|record {} font?;
};

# Represents the Queries record for the operation: listCharts
public type ListChartsQueries record {
    # Skip the first n items
    @http:Query {name: "$skip"}
    int dollarSkip?;
    # Show only the first n items
    @http:Query {name: "$top"}
    int dollarTop?;
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
    # Order items by property values
    @http:Query {name: "$orderby"}
    string[] dollarOrderby?;
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Include count of items
    @http:Query {name: "$count"}
    boolean dollarCount?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: unmergeRangeByAddress
public type UnmergeRangeByAddressHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateColumnFilter
public type UpdateColumnFilterHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: mergeRange
public type MergeRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getColumnFilter
public type GetColumnFilterQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: clearRangeByAddress
public type ClearRangeByAddressHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getUsedRangeSort
public type GetUsedRangeSortHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ChartAxes record {
    *Entity;
    # Represents the category axis in a chart. Read-only
    ChartAxis|record {} categoryAxis?;
    # Represents the value axis in an axis. Read-only
    ChartAxis|record {} valueAxis?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents the series axis of a 3-dimensional chart. Read-only
    ChartAxis|record {} seriesAxis?;
};

# Represents the Headers record for the operation: unmergeRange
public type UnmergeRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateApplication
public type UpdateApplicationHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type Range record {
    *Entity;
    # Indicates whether all columns of the current range are hidden
    boolean? columnHidden?;
    # Represents the range reference in A1-style. Address value contains the Sheet reference (for example, Sheet1!A1:B4). Read-only
    string? address?;
    # Represents the formula in A1-style notation, in the user's language and number-formatting locale.  For example, the English '=SUM(A1, 1.5)' formula would become '=SUMME(A1; 1,5)' in German
    anydata formulasLocal?;
    # Represents if all cells of the current range are hidden. Read-only
    boolean? hidden?;
    # Represents range reference for the specified range in the language of the user. Read-only
    string? addressLocal?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Represents the formula in A1-style notation
    anydata formulas?;
    # Represents the raw values of the specified range. The data returned can be of type string, number, or a Boolean. Cell that contains an error returns the error string
    anydata values?;
    # Returns a format object, encapsulating the range's font, fill, borders, alignment, and other properties. Read-only
    RangeFormat|record {} format?;
    # Represents the total number of columns in the range. Read-only
    decimal columnCount?;
    # The worksheet containing the current range. Read-only
    RangeSort|record {} sort?;
    # Represents the formula in R1C1-style notation
    anydata formulasR1C1?;
    # Indicates whether all rows of the current range are hidden
    boolean? rowHidden?;
    # Number of cells in the range. Read-only
    decimal cellCount?;
    # Represents Excel's number format code for the given cell
    anydata numberFormat?;
    # Represents the column number of the first cell in the range. Zero-indexed. Read-only
    decimal columnIndex?;
    # Returns the row number of the first cell in the range. Zero-indexed. Read-only
    decimal rowIndex?;
    # Represents the type of data of each cell. The possible values are: Unknown, Empty, String, Integer, Double, Boolean, Error. Read-only
    anydata valueTypes?;
    # The worksheet containing the current range. Read-only
    Worksheet|record {} worksheet?;
    # Returns the total number of rows in the range. Read-only
    decimal rowCount?;
    # Text values of the specified range. The Text value doesn't depend on the cell width. The # sign substitution that happens in Excel UI doesn't affect the text value returned by the API. Read-only
    anydata text?;
};

# Represents the Headers record for the operation: listTables
public type ListTablesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type PivotTable record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The name of the pivot table
    string? name?;
    # The worksheet that contains the current pivot table. Read-only
    Worksheet|record {} worksheet?;
};

# Represents the Headers record for the operation: getUsedRange
public type GetUsedRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getWorksheetTable
public type GetWorksheetTableHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ApplyCellColorFilterRequest record {
    string? color?;
};

# Represents the Queries record for the operation: listTables
public type ListTablesQueries record {
    # Skip the first n items
    @http:Query {name: "$skip"}
    int dollarSkip?;
    # Show only the first n items
    @http:Query {name: "$top"}
    int dollarTop?;
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
    # Order items by property values
    @http:Query {name: "$orderby"}
    string[] dollarOrderby?;
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Include count of items
    @http:Query {name: "$count"}
    boolean dollarCount?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: createChartSeries
public type CreateChartSeriesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getNamedItem
public type GetNamedItemQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: getOperation
public type GetOperationHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: applyTopPctFilter
public type ApplyTopPctFilterHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: countChartSeries
public type CountChartSeriesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: countNamedItems
public type CountNamedItemsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: deleteChartSeries
public type DeleteChartSeriesHeaders record {
    # ETag
    @http:Header {name: "If-Match"}
    string ifMatch?;
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ApplyBottomItemsFilterRequest record {
    @constraint:Number {minValue: -2147483648, maxValue: 2147483647}
    decimal count?;
};

# Represents the Queries record for the operation: countNamedItems
public type CountNamedItemsQueries record {
    # Filter items by property values
    @http:Query {name: "$filter"}
    string dollarFilter?;
    # Search items by search phrases
    @http:Query {name: "$search"}
    string dollarSearch?;
};

# Represents the Headers record for the operation: deleteRow
public type DeleteRowHeaders record {
    # ETag
    @http:Header {name: "If-Match"}
    string ifMatch?;
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: listCharts
public type ListChartsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getWorksheetNamedItem
public type GetWorksheetNamedItemHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type RangeFormat record {
    *Entity;
    # The horizontal alignment for the specified object. The possible values are: General, Left, Center, Right, Fill, Justify, CenterAcrossSelection, Distributed
    string? horizontalAlignment?;
    # Collection of border objects that apply to the overall range selected Read-only
    RangeBorder[] borders?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The width of all columns within the range. If the column widths aren't uniform, null will be returned
    decimal|string|ReferenceNumeric? columnWidth?;
    # Returns the format protection object for a range. Read-only
    FormatProtection|record {} protection?;
    # Returns the fill object defined on the overall range. Read-only
    RangeFill|record {} fill?;
    # Indicates whether Excel wraps the text in the object. A null value indicates that the entire range doesn't have a uniform wrap setting
    boolean? wrapText?;
    # The vertical alignment for the specified object. The possible values are: Top, Center, Bottom, Justify, Distributed
    string? verticalAlignment?;
    # The height of all rows in the range. If the row heights aren't uniform null will be returned
    decimal|string|ReferenceNumeric? rowHeight?;
    # Returns the font object defined on the overall range selected Read-only
    RangeFont|record {} font?;
};

public type ApplyCustomFilterRequest record {
    string? criteria1?;
    string? criteria2?;
    string oper?;
};

# Represents the Headers record for the operation: createOperation
public type CreateOperationHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: createRow
public type CreateRowHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type Operation record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The error returned by the operation
    OperationError|record {} 'error?;
    # The resource URI for the result
    string? resourceLocation?;
    OperationStatus status?;
};

public type ChartFill record {
    *Entity;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
};

public type WorksheetProtectionOptions record {
    # Indicates whether the worksheet protection option to allow the use of the pivot table feature is enabled
    boolean allowPivotTables?;
    # Indicates whether the worksheet protection option to allow deleting columns is enabled
    boolean allowDeleteColumns?;
    # Indicates whether the worksheet protection option to allow deleting rows is enabled
    boolean allowDeleteRows?;
    # Indicates whether the worksheet protection option to allow formatting cells is enabled
    boolean allowFormatCells?;
    # Indicates whether the worksheet protection option to allow inserting rows is enabled
    boolean allowInsertRows?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Indicates whether the worksheet protection option to allow the use of the autofilter feature is enabled
    boolean allowAutoFilter?;
    # Indicates whether the worksheet protection option to allow formatting rows is enabled
    boolean allowFormatRows?;
    # Indicates whether the worksheet protection option to allow inserting hyperlinks is enabled
    boolean allowInsertHyperlinks?;
    # Indicates whether the worksheet protection option to allow inserting columns is enabled
    boolean allowInsertColumns?;
    # Indicates whether the worksheet protection option to allow formatting columns is enabled
    boolean allowFormatColumns?;
    # Indicates whether the worksheet protection option to allow the use of the sort feature is enabled
    boolean allowSort?;
};

# Represents the Headers record for the operation: updateUsedRangeFormat
public type UpdateUsedRangeFormatHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type AddRowRequest record {
    anydata values?;
    decimal? index?;
};

public type ChartAxis record {
    *Entity;
    # Returns a Gridlines object that represents the minor gridlines for the specified axis. Read-only
    ChartGridlines|record {} minorGridlines?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # Returns a gridlines object that represents the major gridlines for the specified axis. Read-only
    ChartGridlines|record {} majorGridlines?;
    # Represents the formatting of a chart object, which includes line and font formatting. Read-only
    ChartAxisFormat|record {} format?;
    # Represents the maximum value on the value axis.  Can be set to a numeric value or an empty string (for automatic axis values).  The returned value is always a number
    anydata maximum?;
    # Represents the axis title. Read-only
    ChartAxisTitle|record {} title?;
    # Represents the minimum value on the value axis. Can be set to a numeric value or an empty string (for automatic axis values).  The returned value is always a number
    anydata minimum?;
    # Represents the interval between two minor tick marks. 'Can be set to a numeric value or an empty string (for automatic axis values). The returned value is always a number
    anydata minorUnit?;
    # Represents the interval between two major tick marks. Can be set to a numeric value or an empty string.  The returned value is always a number
    anydata majorUnit?;
};

# Represents the Headers record for the operation: countPivotTables
public type CountPivotTablesHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateRangeSort
public type UpdateRangeSortHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: createComment
public type CreateCommentHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getColumn
public type GetColumnHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type Comment record {
    *Entity;
    # The list of replies to the comment. Read-only. Nullable
    CommentReply[] replies?;
    @jsondata:Name {value: "@odata.type"}
    string atOdataType?;
    # The content type of the comment
    string contentType?;
    # The content of the comment
    string? content?;
};

# Represents the Headers record for the operation: getTableTotalRowRange
public type GetTableTotalRowRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type AddTableResponse Table|AddTableResponseEmpty?;

# Represents the Headers record for the operation: deleteReply
public type DeleteReplyHeaders record {
    # ETag
    @http:Header {name: "If-Match"}
    string ifMatch?;
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: getRangeByAddressSort
public type GetRangeByAddressSortHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type DeleteRangeRequest record {
    string shift?;
};

# Represents the Headers record for the operation: listRows
public type ListRowsHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getTableWorksheet
public type GetTableWorksheetQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

# Represents the Headers record for the operation: getCategoryAxis
public type GetCategoryAxisHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type ChartImageResponse record {
    string? value?;
};

# Represents the Headers record for the operation: updateRow
public type UpdateRowHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

public type AddChartResponse Chart|AddChartResponseEmpty?;

# Represents the Headers record for the operation: addNamedItemFormulaLocal
public type AddNamedItemFormulaLocalHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Queries record for the operation: getChartAxes
public type GetChartAxesQueries record {
    # Expand related entities
    @http:Query {name: "$expand"}
    string[] dollarExpand?;
    # Select properties to be returned
    @http:Query {name: "$select"}
    string[] dollarSelect?;
};

public type AddColumnResponse TableColumn|AddColumnResponseEmpty?;

# Represents the Headers record for the operation: getColumnDataBodyRange
public type GetColumnDataBodyRangeHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};

# Represents the Headers record for the operation: updateTableSort
public type UpdateTableSortHeaders record {
    # Workbook session ID returned by createSession. Pass it to run the request inside a persistent or non-persistent Excel session
    @http:Header {name: "workbook-session-id"}
    string workbookSessionId?;
};
